# Environment variables template for ss-api
# Copy this file to .envrc and update the values for your environment
# Note: .envrc is used by direnv and should not be committed to git

# Database Configuration
DB_URL=jdbc:postgresql://localhost:5432/ss_api
DB_USERNAME=your_db_username
DB_PASSWORD=your_db_password

# Redis Configuration
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DATABASE=0
REDIS_TIMEOUT=2000

# JWT Configuration (IMPORTANT: Use strong secrets in production)
JWT_SECRET=your-secure-jwt-secret-key-min-32-chars
JWT_ACCESS_EXPIRATION=900
JWT_REFRESH_EXPIRATION=2592000
JWT_ISSUER=ss-api

# Play Framework Configuration (IMPORTANT: Use strong secret in production)
PLAY_SECRET_KEY=your-secure-play-secret-key-min-32-chars
PLAY_HTTP_PORT=9000

# Docker Compose Environment Variables
POSTGRES_USER=your_postgres_user
POSTGRES_PASSWORD=your_postgres_password
POSTGRES_DB=your_database_name

# Development flags
PLAY_EVOLUTIONS_ENABLED=true
PLAY_EVOLUTIONS_AUTO_APPLY=true

# Instructions:
# 1. Install direnv: https://direnv.net/docs/installation.html
# 2. Copy this file: cp .env.example .envrc
# 3. Update the values above with your configuration
# 4. Allow direnv: direnv allow
# 5. The environment variables will be automatically loaded when you cd into this directory